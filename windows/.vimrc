" Windows vim config file

" Set compatibility to Vim only.
set number
" set nolist " Hide hidden characters.
set rnu " relative number (numbers on left bar) 

" Helps force plug-ins to load correctly when it is turned back on below.
filetype off


" Turn on the syntax highlighting.
syntax on  

" map &lt;C-n> :NERDTreeToggle&lt;CR> " No instalado

imap ii <Esc>
